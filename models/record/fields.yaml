# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: 'dimsog.subscription::lang.models.general.id'
        disabled: true
